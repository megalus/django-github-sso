[build-system]
requires = ["hatchling>=1.27.0"]
build-backend = "hatchling.build"

[tool.hatch.version]
path = "django_github_sso/__init__.py"

[tool.hatch.build.targets.wheel]
packages = ["django_github_sso"]

[tool.semantic_release]
version_variables = [
    "django_github_sso/__init__.py:__version__",
]
branch = "main"
upload_to_pypi = false
upload_to_release = true
build_command = "uv build"

[project]
name = "django-github-sso"
dynamic = ["version"]
description = "Easily add GitHub Authentication to your Django Projects"
readme = "README.md"
requires-python = ">=3.11, <4.0"
license = { text = "MIT" }
authors = [{ name = "Chris Maillefaud", email = "chrismaille@users.noreply.github.com" }]
keywords = ["github", "django", "sso"]
classifiers = [
    "Framework :: Django",
    "Framework :: Django :: 5.0",
    "Framework :: Django :: 5.1",
    "Framework :: Django :: 5.2",
    "Framework :: Django :: 6.0",
    "Intended Audience :: Developers",
    "Development Status :: 4 - Beta",
    "Environment :: Plugins",
]
dependencies = [
    "django>=5.0",
    "loguru",
    "requests-oauthlib",
    "PyGithub",
    "httpx"
]

[project.urls]
Homepage = "https://github.com/megalus/django-github-sso"
Repository = "https://github.com/megalus/django-github-sso"
Documentation = "https://megalus.github.io/django-github-sso/"

[dependency-groups]
dev = [
    "arrow",
    "auto-changelog",
    "bandit",
    "click>8",
    "django-admin-interface",
    "django-grappelli",
    "django-jazzmin",
    "django-jet-reboot",
    "django-unfold",
    "faker",
    "httpx",
    "mkdocs-material",
    "mkdocs-mermaid2-plugin",
    "pre-commit",
    "pytest-asyncio",
    "pytest-coverage",
    "pytest-django",
    "pytest-mock",
    "python-dotenv",
    "ruff",
    "stela",
    "django-google-sso",
    "django-microsoft-sso"
]

[tool.ruff]
line-length = 92
target-version = "py311"
exclude = [
    ".git",
    ".venv",
    "venv",
    "build",
    "dist",
]

[tool.ruff.lint]
exclude = ["django_github_sso/migrations"]

[tool.ruff.format]
exclude = ["django_github_sso/migrations"]

[tool.stela]
environment_variable_name = "STELA_ENV"
evaluate_data = true
show_logs = false
env_file = ".env"
config_file_path = "./example_github_app"
